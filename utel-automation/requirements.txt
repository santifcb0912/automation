# ============================================================
# requirements.txt
# Lista de todas las librerías que necesita el proyecto
# Para instalar: pip install -r requirements.txt
# ============================================================

# Servidor web y API REST — equivalente a Spring Boot en Java
fastapi==0.110.0

# Servidor que corre FastAPI — equivalente a Tomcat en Java
uvicorn==0.27.1

# Para renderizar el HTML de la interfaz gráfica
jinja2==3.1.3

# Para manejar las respuestas de formularios HTML
python-multipart==0.0.9

# Automatización del navegador — el motor del scraping
playwright==1.42.0

# Librería para evitar que Cloudflare detecte que somos un bot
playwright-stealth==1.0.6

# Librería para leer y escribir en Google Sheets
gspread==6.0.2

# Librería para autenticarse con Google (Sheets + Drive)
google-auth==2.28.2

# Librería oficial de Google para Drive y Sheets
google-api-python-client==2.120.0

# Para cargar las variables del archivo .env
python-dotenv==1.0.1

# Para hacer llamadas HTTP (usado para subir imágenes a Drive)
httpx==0.27.0

# Para logs con colores y formato — mejor que el print() básico
loguru==0.7.2

# Para manejar fechas y horas fácilmente
python-dateutil==2.9.0
